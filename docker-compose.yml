services:
  telegram-crypto-price-bot:
    build: .
    container_name: telegram_crypto_price_bot_container
    environment:
      - CONFIG_FILE=${CONFIG_FILE:-conf/config.ini}
      - TZ=Europe/Rome
    restart: unless-stopped
    stop_grace_period: 10s

    deploy:
      resources:
        limits:
          memory: 512M

    volumes:
      - ./app/conf:/code/conf:ro
      - ./app/lang:/code/lang:ro
      - ./app/session:/code/session
      - ./app/logs:/code/logs

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
